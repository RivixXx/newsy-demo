[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run db:migrate && npm run db:seed && next start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
