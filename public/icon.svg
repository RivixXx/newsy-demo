<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <style>
      .chi-letter { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif; font-size: 52px; font-weight: 900; fill: white; transform-box: fill-box; transform-origin: center; }
      @keyframes spinLeft { to { transform: rotate(-360deg); } }
      @keyframes spinRight { to { transform: rotate(360deg); } }
      .spin-c { animation: spinLeft 7s linear infinite; }
      .spin-i { animation: spinRight 7s linear infinite; }
    </style>
  </defs>
  <rect width="100" height="100" rx="22" fill="#E11D48"/>
  <text class="chi-letter spin-c" x="38" y="67">Ч</text>
  <text class="chi-letter spin-i" x="62" y="67" letter-spacing="-2">И</text>
</svg>
